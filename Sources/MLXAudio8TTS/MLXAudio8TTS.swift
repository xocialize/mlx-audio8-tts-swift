// MLXAudio8TTS — engine-facing wrapper (Stage 2). Placeholder pending the
// ModelPackage conformance work; Audio8TTSCore is the Stage 1 surface.

public enum MLXAudio8TTS {
    public static let version = "0.0.1"
}
